
from datetime import datetime

note_list = []
# приветствие программы
print('Добро пожаловать в менеджер заметок! Вы можете добавить новую заметку.')

# Реализуем цикл, который позволяет пользователю добавлять любое количество заметок.

while True:
    new_note = input('Хотите добавить ещё одну заметку? (да/нет): ')
    if new_note == 'да':
        while True:
            username = input("Введите имя пользователя: ")
            if not username:
                print("Имя пользователя не может быть пустым!")
            else: # Исключаем ввод пустого значения
                break

        while True:
            title = input('Введите заголовок заметки: ')
            if not title:
                print("Заголовок заметки не может быть пустым!")
            else:  # Исключаем ввод пустого значения
                break

        while True:
            content = input("Введите описание заметки: ")
            if not content:
                print("Описание заметки не может быть пустым")
            else:
                break

# статусы
        while True:
            status = input("Введите статус заметки (например, 'Выполнено', 'В процессе', 'Отложено'): ")
            status = ["1: выполнено", "2: в процессе", "3: отложено"]
            if status = 

            # note['status'] = input("Введите статус заметки (например, 'Выполнено', 'В процессе', 'Отложено'): ")
            # note['created_date'] = input("Введите дату создания заметки в формате 'день-месяц-год': ")
            # note['issue_date'] = input("Введите дату истечения заметки в формате 'день-месяц-год': ")




    if new_note == 'нет':
        break

note = {"Имя пользователя": username, "Заголовок заметки": note_title, "Описание заметки": note_content, "Статус заметки": str(note_status), "Дата создания": str(note_created_date), "Дедлайн": str(note_issue_date)}
